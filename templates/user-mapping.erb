<user-mapping>

<% @students.each do |student| -%>
<authorize username="<%= student %>" password="puppetlabs">
  <connection name="<%= student %>-win">        
    <protocol>rdp</protocol>
    <param name="hostname"><%= student %>-win</param>
    <param name="port">3389</param>
    <param name="username"><%= student %></param>
    <param name="password">P4ssW0rd!</param>
    <param name="security">tls</param>
    <param name="ignore-cert">true</param>
    <param name="width">1280</param>
    <param name="height">720</param>
    <param name="color">8</param>
  </connection>
  <connection name="<%= student %>-linux">        
    <protocol>ssh</protocol>
    <param name="hostname"><%= student %>-linux</param>
    <param name="username"><%= student %></param>
    <param name="password">P4ssW0rd!</param>
  </connection>
</authorize>

<% end -%>

</user-mapping>
